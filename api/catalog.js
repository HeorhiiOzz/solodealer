const crypto=require('crypto');const seed=require('../seed.json');const HASH='7a6dc546069b028304e9bf001a98a5e47093f88b69cb62e8fce2fc5b56a7b379';
function okPass(req){let p=req.headers['x-admin-password']||'';return crypto.createHash('sha256').update(String(p)).digest('hex')===HASH}
async function readCars(){if(!process.env.BLOB_READ_WRITE_TOKEN)return seed;const {list,put}=await import('@vercel/blob');let r=await list({prefix:'catalog/catalog-',limit:100,token:process.env.BLOB_READ_WRITE_TOKEN});if(!r.blobs?.length)return seed;let b=[...r.blobs].sort((a,b)=>new Date(b.uploadedAt)-new Date(a.uploadedAt))[0];let fr=await fetch(b.url,{cache:'no-store'}),cars=await fr.json();if(!Array.isArray(cars))return seed;let ids=new Set(cars.map(c=>c.id)),missing=seed.filter(c=>!ids.has(c.id));if(missing.length){cars=[...cars,...missing];await put(`catalog/catalog-${Date.now()}.json`,JSON.stringify(cars),{access:'public',addRandomSuffix:false,contentType:'application/json',cacheControlMaxAge:60,token:process.env.BLOB_READ_WRITE_TOKEN})}return cars}
module.exports=async(req,res)=>{try{if(req.method==='GET'){let cars=await readCars();if(req.query.admin==='1'){if(!okPass(req))return res.status(401).json({error:'Невірний пароль'});return res.status(200).json({cars,storage:!!process.env.BLOB_READ_WRITE_TOKEN})}return res.status(200).json(cars.filter(c=>c.published!==false))}
if(req.method==='POST'){if(!okPass(req))return res.status(401).json({error:'Невірний пароль'});if(!process.env.BLOB_READ_WRITE_TOKEN)return res.status(503).json({error:'Спочатку підключи Vercel Blob у Storage'});let cars=req.body?.cars;if(!Array.isArray(cars))return res.status(400).json({error:'Некоректний каталог'});const {put}=await import('@vercel/blob');await put(`catalog/catalog-${Date.now()}.json`,JSON.stringify(cars),{access:'public',addRandomSuffix:false,contentType:'application/json',cacheControlMaxAge:60,token:process.env.BLOB_READ_WRITE_TOKEN});return res.status(200).json({ok:true})}res.status(405).json({error:'Method not allowed'})}catch(e){console.error(e);res.status(500).json({error:'Помилка сервера'})}}
const crypto=require('crypto');const HASH='7a6dc546069b028304e9bf001a98a5e47093f88b69cb62e8fce2fc5b56a7b379';
function okPass(req){let p=req.headers['x-admin-password']||'';return crypto.createHash('sha256').update(String(p)).digest('hex')===HASH}
module.exports=async(req,res)=>{try{if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!okPass(req))return res.status(401).json({error:'Невірний пароль'});if(!process.env.BLOB_READ_WRITE_TOKEN)return res.status(503).json({error:'Спочатку підключи Vercel Blob у Storage'});let {base64,filename}=req.body||{};if(!base64)return res.status(400).json({error:'Фото відсутнє'});let buf=Buffer.from(base64,'base64');if(buf.length>3500000)return res.status(413).json({error:'Фото завелике'});const {put}=await import('@vercel/blob');let safe=String(filename||'photo.jpg').replace(/[^a-zA-Z0-9._-]/g,'_');let b=await put(`cars/${Date.now()}-${safe}`,buf,{access:'public',addRandomSuffix:true,contentType:'image/jpeg',token:process.env.BLOB_READ_WRITE_TOKEN});res.status(200).json({url:b.url})}catch(e){console.error(e);res.status(500).json({error:'Помилка завантаження'})}}